probe begin {
	printf("curr sctx %s  \n", sid_to_context_str( pid2sid(pid())))
#change_task_sid(task_current(),49)
#
	chang_curr_sid(2165)
	for(sid = 2150; sid < 2168; sid ++){
		printf("%d:%s \n", sid, sid_to_context_str(sid))
	}
	printf("curr sid %d \n", pid2sid(pid()))
	exit()
}
